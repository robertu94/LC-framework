@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/LCTargets.cmake")
find_package(OpenMP REQUIRED)
find_package(Python REQUIRED)

# document what components are available
set(LC_CPU_FOUND ON)
if(@LC_BUILD_LIBPRESSIO_PLUGIN@)
    set(LC_LIBPRESSIO_CPU_FOUND ON)
    if(@LC_BUILD_CUDA@)
        set(LC_LIBPRESSIO_GPU_FOUND ON)
    endif()
endif()
if(@LC_BUILD_CUDA@)
    set(LC_GPU_FOUND ON)
endif()

if(@LC_BUILD_LIBPRESSIO_PLUGIN@)
    find_package(std_compat REQUIRED)
    find_package(LibPressio REQUIRED)
endif()
check_required_components(LC)
